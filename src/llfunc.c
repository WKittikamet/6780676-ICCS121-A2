#include <stdio.h>
#include <stdlib.h>
#include <linkedlist.h>

// Add a node to the list with information at position pos
void add(Node *list, long value, char *words, int pos){

};
// Delete node at position pos
void dlt(Node *list, int pos){

};

// Clears all memory allocations
void clear_memory(Node *list){

};
